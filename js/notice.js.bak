$(document).ready(function(){
	parser = new DomParser();
	xmlDoc = parser.parseFromString(text, "xml/notice.xml");

	$("#print").append(xmlDoc.getElementsByTagName("name")[0].childNodes[0].nodeValue);
	
});
